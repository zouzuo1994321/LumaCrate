# 流明盒 LumaCrate · 性能基线

> 由 `dev/benchmark.py` 在**真实索引**上生成（只读，不写入）。
> 生成时间：2026-09-21 11:42:16 ｜ 被测版本：**v1.26.0 (Build 2609210036)**

## 一、测试环境

| 项 | 值 |
|---|---|
| CPU | 13th Gen Intel(R) Core(TM) i5-13400F |
| 核心数 | 16 |
| 内存 | 31.8 GB |
| 操作系统 | Windows 11 |
| Python | 3.13.14 (AMD64) |
| PySide6 | 6.11.2 |

## 二、数据规模

| 项 | 值 |
|---|---|
| 数据库文件 | `Z:\【01】自研软件\【26-19】本地影视中心\index_data\media_center.db` |
| 文件体积 | 128.9 MB |
| 影片 | 48083 |
| 剧集 | 0 |
| 分片（选集） | 1585 |
| 演员 / 影人 | 7127 |
| media 行数 | 49668 |
| people 行数 | 7127 |
| media_people 关联 | 92590 |
| 索引条数 | 24 |
| journal 模式 | wal |
| 有封面的条目 | 49484 |
| 有 nfo 的条目 | 49662 |
| 媒体库 | 2 个 |

## 三、结果

### 数据层（单位 ms，取中位数）

| 用例 | 规模 | 中位 | 最小 | 最大 | n | 阈值 | 判定 | 说明 |
|---|---|---:|---:|---:|---:|---:|:--:|---|
| 冷启动首个查询（含建连） | — | **59.98** | 56.76 | 61.44 | 3 | 300 | ✅ | 应用启动时第一次碰库的代价 |
| 全库计数 count_media() | 48k | **0.02** | 0.01 | 0.02 | 5 | 80 | ✅ |  |
| 关键词计数 count_media(kw) | — | **27.67** | 27.57 | 28.02 | 5 | 250 | ✅ |  |
| 组合筛选计数（标签+年份） | — | **24.04** | 23.75 | 24.07 | 5 | 120 | ✅ |  |
| 单库计数 count_media(library) | — | **0.07** | 0.07 | 0.08 | 5 | 80 | ✅ |  |
| 首屏分页 60 条（light） | offset=0 | **0.65** | 0.62 | 0.86 | 5 | 80 | ✅ |  |
| 深层翻页 60 条 | offset=49548 | **2.43** | 2.36 | 3.08 | 5 | 150 | ✅ | 滚到底部/自动续拉 |
| 随机排序 60 条（深 offset） | — | **219.26** | 218.67 | 227.08 | 5 | 180 | ⚠️ | 会话种子置换，不是 ORDER BY RANDOM() |
| 最近添加 60 条（按时间倒序） | — | **0.69** | 0.65 | 0.95 | 5 | 100 | ✅ |  |
| 关键词列表 60 条 | — | **0.71** | 0.65 | 0.90 | 5 | 300 | ✅ |  |
| 单库列表 60 条 | — | **2.46** | 2.37 | 2.97 | 5 | 100 | ✅ |  |
| 侧边栏库名计数 library_counts() | — | **22.09** | 21.71 | 23.03 | 5 | 80 | ✅ |  |
| 总览 stats() | — | **16.36** | 16.04 | 16.89 | 5 | 120 | ✅ |  |
| 详情页 PK 回查 ×40 | — | **0.94** | 0.93 | 1.53 | 5 | 200 | ✅ | 点开一张卡打开详情 |
| 批量演员映射 actors_map(60) | — | **0.14** | 0.13 | 0.17 | 5 | 100 | ✅ | 一屏 60 张卡的演员小字 |
| 批量演职员 cast_crew_map(60) | — | **0.16** | 0.16 | 0.17 | 5 | 100 | ✅ |  |
| 按 id 回填整行 media_by_ids(60) | — | **0.70** | 0.66 | 0.97 | 5 | 100 | ✅ | 智能推荐结果喂给卡片墙前的回填 |
| 演员列表 query_people(100) | — | **50.95** | 50.63 | 51.39 | 5 | 300 | ✅ | 演员库首屏 |
| 演员计数 count_people(Actor) | — | **43.47** | 42.69 | 45.47 | 5 | 150 | ✅ |  |
| nfo 解析（20 份合计） | — | **69.09** | 67.83 | 73.01 | 5 | 600 | ✅ | 扫描时逐文件解析 nfo |
| 全库画像 insight.Portrait().build() | — | **1254.10** | 1254.10 | 1254.10 | 1 | — | — | 「画像概览」页；后台线程 + 进度回调 |
| 全库标签统计 library_tag_stats() | — | **2257.49** | 2257.49 | 2257.49 | 1 | — | — | 「标签优化」扫描预览；后台线程 |
| TagOptimizer 构造 | — | **0.00** | 0.00 | 0.00 | 3 | 300 | ✅ |  |

### Qt 层（单位 ms，取中位数）

| 用例 | 规模 | 中位 | 最小 | 最大 | n | 阈值 | 判定 | 说明 |
|---|---|---:|---:|---:|---:|---:|:--:|---|
| QApplication 初始化 | — | **6.61** | 6.61 | 6.61 | 1 | 2000 | ✅ | 进程启动 → Qt 就绪 |
| 样式表渲染 render_style() | — | **1.51** | 1.37 | 2.32 | 20 | 30 | ✅ | 换高亮色时的字符串替换 |
| 样式表应用 load_style(app) | — | **1.72** | 1.45 | 6.56 | 10 | 200 | ✅ | 整套 QSS 重解析（换色/换主题） |
| 建 60 张海报卡（冷封面缓存） | — | **390.05** | 389.04 | 391.13 | 5 | 1200 | ✅ | 首屏：含封面解码+缩放 |
| 建 60 张海报卡（热缓存） | — | **48.02** | 47.08 | 49.27 | 5 | 600 | ✅ | 续拉稳态：封面已缓存 |
| 自绘开关渲染 ×200（每帧现取高亮色） | — | **6.87** | 6.87 | 6.97 | 3 | 600 | ✅ | 单帧 0.034 ms |

## 四、结论

### 4.1 交互路径都落在「无感」区间

- 首屏分页、深层翻页、最近添加、关键词搜索、单库筛选、批量演员映射、主键回查，全部在 **个位数到几十毫秒**；这些是滚动和翻页时会反复发生的操作，保持在这个量级用户察觉不到停顿。
- 跨全表聚合（`count_media(keyword)` / `library_counts()` / `stats()` / `count_people()`）落在 15~50 ms：它们**必然**要全扫一遍，所以合适的位置是「进页面时算一次 + 结果缓存」，不要挂到输入框的每次改动上。

### 4.2 必须放后台线程的操作（写死这条）

- `insight.Portrait().build()` / `tagopt.library_tag_stats()` 是**全库全表扫描**，必须留在后台线程并给进度回调；它们出现在 GUI 线程里就是一次可见的卡死。
- **卡片墙渲染**：PosterCard 每张 ≈ 0.8 ms（热缓存）—— 一屏 60 张 ≈ 48 ms，所以卡片墙必须增量渲染（`LazyGrid` 每批 60 + `QTimer.singleShot(1)` 让出事件循环），一次性建满 4.8 万张会直接冻住界面。

### 4.3 卡片墙的规模上限来自「控件数」而不是「查询」

数据层已经下推 SQL（筛选/排序/分页全在库内完成，不把整库读进内存），真正的瓶颈是**每张卡都是一个 QFrame + 若干 QLabel**。本基线给出的是「每批 60 张」的代价，`LazyGrid` 按此每次只建一批、用 `QTimer.singleShot(1, …)` 让出事件循环，因此 4.8 万条与 5 千条的首屏体感一致。

### 4.4 已知边界：随机排序是「全表扫描 + 全表排序」

`sort="random"` 走的是会话种子置换 `((id * 2654435761 + SEED) % 2147483647)`。它是一个**表达式**，任何索引都无法排序它，所以每取一页都要把符合条件的行全扫一遍再排一次 —— 实测 **219 ms**，且随 offset 增大而变差（浅 offset 靠 top-N 排序器侥幸快，深 offset 必须整表排序）。

这是**设计上的取舍**，不是 bug：换来的是「分页稳定、不重不漏、点一下重新洗牌」。如果将来要优化，成本最低的路径是「物化一列 + 索引」：给 `media` 加一列 `shuffle INTEGER` + `CREATE INDEX … ON media(shuffle, id)`，`reshuffle()` 时整表 `UPDATE media SET shuffle = (id * MUL + SEED) % MOD`（一次性、约等于一次全表扫），之后每页都能走索引，落到个位数毫秒。代价是 `media` 多一列、插入新行时要一并写好该列。

### 4.5 本次基准测试直接带出的优化（已修）

第一轮跑出来的数字里，**排序相关的用例全是「全表扫描 + 临时 B-Tree 排序」**（`EXPLAIN QUERY PLAN` 为证：`SCAN m` + `USE TEMP B-TREE FOR ORDER BY`）。

根因不是 SQL 写得差，而是**索引与 ORDER BY 对不上**：

1. `search_media` 的排序键永远带附加项 —— `ORDER BY <字段> <方向>, m.year DESC, m.id ASC`；
2. 已有的 `idx_media_sort_title` 是**单列 + BINARY collation**，而查询写的是 `m.sort_title COLLATE NOCASE`。

于是任何一条影片墙查询都无法命中索引。补两条与 ORDER BY **逐列对齐**的复合索引后：

| 用例 | 修复前 | 修复后 | 提速 |
|---|---:|---:|---:|
| 深层翻页 60 条（名称升序，offset 47963） | 249.26 ms | 2.43 ms | 103× |
| 最近添加 60 条（按时间倒序） | 151.54 ms | 0.69 ms | 218× |
| 首屏分页 60 条 | 14.88 ms | 0.65 ms | 23× |
| 关键词列表 60 条 | 31.42 ms | 0.71 ms | 44× |

代价：索引让数据库体积从 121.0 MB 涨到 128.9 MB（+7.9 MB），`init_db()` 首次补索引耗时约 4 s（仅一次，之后启动无感）。

### 4.6 复现方式

```bash
"C:/Users/zouzu/.workbuddy/binaries/python/envs/default/Scripts/python.exe" -u -c \
  "import runpy; runpy.run_path(r'<项目根>/dev/benchmark.py', run_name='__main__')"
```

同一台机器上数字应在 ±20% 内浮动。换机器、或媒体库放在 NAS/网络盘上时差异会更大，因为封面解码与 nfo 解析是**真的落盘 I/O**，本基线也把它们算进去了。

### 4.7 本次样本参数

| 参数 | 值 |
|---|---|
| 关键词样本 | `01` |
| 流派样本 | `如月结衣` |
| 年份样本 | `2020` |
| 媒体库样本 | `Jav-VR` |
